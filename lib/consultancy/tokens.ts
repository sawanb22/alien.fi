export const MN =
  "var(--font-azeret), ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace";
export const SN =
  "var(--font-poppins), system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, sans-serif";

/**
 * Azeret Mono advances the space character like a full glyph; multi-word display
 * type reads with oversized gaps. Slight negative word-spacing restores a
 * proportional rhythm without changing the font.
 */
export const MN_WORD_SPACE = "-0.065em";

/** Horizontal page gutter (matches legacy `OT` in shared HTML). */
export const OT = 51;

/** Content width sidebar column (Meridian bespoke hero grid). */
export const CW = 309;
