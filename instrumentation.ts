/**
 * Next.js instrumentation hook (project root).
 * Turbopack dev resolves this module; without a file, `next dev` throws
 * MODULE_UNPARSABLE: Could not parse module '[project]/instrumentation.ts', file not found.
 */
export async function register() {}
